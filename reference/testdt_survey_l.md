# Test survey

Same randomly generated survey data, one row per person/wave (long
format)

## Usage

``` r
testdt_survey_l
```

## Format

An object of class `tbl_df` (inherits from `tbl`, `data.frame`) with 15
rows and 7 columns.
